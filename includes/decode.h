#ifndef __DECODE_H
#define __DECODE_H

// Alfabeto
void imprime_alfabeto(NO *raiz, int codigo);
// Descompressão
void descomprime(ARVORE_PREFIXO arvore, char* comprimida);
// Auxiliar / Impressão
void tabela();

#endif